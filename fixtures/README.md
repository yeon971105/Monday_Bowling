# PDF fixture

Place the real weekly BLS league sheet at `fixtures/OH260716-10.pdf` to enable the end-to-end PDF fixture test. PDF files are intentionally ignored so league data is not committed accidentally.
